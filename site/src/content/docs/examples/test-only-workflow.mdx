---
title: Test-Only Workflow
description: Use 202 then 302 when the behavior already exists but the automated coverage does not.
section: examples
order: 4
sidebar:
  order: 4
---

import PromptBlock from '../../../components/PromptBlock.astro';
import Callout from '../../../components/Callout.astro';

## When this applies

Use this workflow when production behavior is already implemented and the real problem is missing or weak automated coverage. The workflow consists of running `202` to generate Gherkin scenarios from the existing code, followed by `302` to implement those scenarios.

## Example prompt sequence

First, generate the test scenarios based on the existing implementation:

<PromptBlock title="Step 1: Generate specs" text={`Run 202 to analyze the existing code to determine what the end-to-end user test scenarios should be for this implemented feature. Identify any potential gaps or inconsistencies in the implementation that we need to make sure we're testing.`} />

Review the generated feature file output. Once you are satisfied with the scenarios, proceed to implement the tests. You can target specific scenarios or run them all at once:

<PromptBlock title="Step 2: Implement a specific test" text={`Run 302 for test number 2 within the billing retry feature.`} />

<PromptBlock title="Step 2 (Alternative): Implement all tests" text={`Run 302 for all tests in @.specflow/features/[feature-name]`} />

## Why not 301?

`301` is for substantial implementation work. `302` exists to keep test-only work from silently turning into broader production changes.

If the current implementation disagrees with the requested scenarios, fix the mismatch first instead of normalizing it inside `302`.

<Callout tone="tip" title="Boundary rule">
  If the tests reveal missing behavior rather than missing coverage, stop and switch back to `301-spec-implementation`.
</Callout>
